version: '3'
services:
  api:
    build:
      context: ./backend
    container_name: api
    ports:
      - '3001:3001'
  ui:
    build:
      context: ./frontend
    container_name: ui
    ports:
      - '3000:3000'
  nginx:
    container_name: nginx
    depends_on:
      - ui
      - api
    ports:
      - '80:80'
    build:
      context: ./nginx
